# Handoff — agent-guardrails, 2026-09-03

Session ended mid-project (credits). This file is the cold-start brief to resume.

## What this project is

Extract Carlitos's three hand-ported AI-coding-agent guardrail hooks (Claude Code
shell hooks → opencode JS plugin → Antigravity Python hook, all in
`~/projects/CtrlCarlitos/takumi-dream/{.claude,.opencode,.agents}`) into one
reusable, dotfiles-installed system: **one Guardrail Policy, many planes.**

Full design: [`DESIGN.md`](../DESIGN.md). Glossary: [`CONTEXT.md`](../CONTEXT.md).
Rationale: [`docs/adr/0001-0003`](./adr/).

## Original 2026-09-03 snapshot

At handoff time, the repo at `~/projects/CtrlCarlitos/agent-guardrails` was
git-initialised on branch `main` with **no remote yet**. Its commits were:

- `a6e485d` — scaffold (DESIGN.md, CONTEXT.md, 3 ADRs, dir skeleton, README, go.mod, guardrail.toml.example)
- `ed807c7` — Plan 1 document
- `facfc85` — this handoff

**Nothing was implemented yet at that point.** The dir skeleton (`cmd/ internal/{policy,engine,adapter} adapters/opencode recipes/ test/fixtures/`) had `.gitkeep`/stub files only.

**Confirmed by Carlitos:**
- repo path `~/projects/CtrlCarlitos/agent-guardrails`
- Go module path `github.com/CtrlCarlitos/agent-guardrails` (locked — `go.mod` already uses it)
- **Create the GitHub remote.** First action on resume:
  ```bash
  gh repo create CtrlCarlitos/agent-guardrails --private --source=. --remote=origin
  ```
  Private (it's security-guardrail logic; can go public later). Do **not** `git push` yet —
  push after Plan 1 is green, not the bare scaffold.

## Environment facts

- **Go is installed at `/usr/local/go/bin/go` (1.25.0) but NOT on the interactive PATH.** The Plan and Makefile invoke it by full path. Add `export PATH=$PATH:/usr/local/go/bin` or use `/usr/local/go/bin/go` directly.
- `git` is on PATH.
- Primary machine is WSL2 Ubuntu 24.04. Windows native is a supported target too.
- Carlitos's dotfiles repo: `~/.local/share/chezmoi` (this session's cwd). Its installer is `run_onchange_install_packages.sh.tmpl` (+ `.ps1.tmpl`), with `~/scripts/update_ai_tools.sh` as the manual updater twin. Skills/superpowers install globally via that installer — the hooks package will follow the same pattern (Plan 2).

## The confirmed design (20 grilled decisions)

| Q | Decision |
|---|---|
| Q1 | Policy = **P1–P10 core**; P11 SQL-guard + docker-container-naming = project **overlay**; P12 per-worktree config-gen = later. Filter for "core": *useful to any developer, not just Carlitos*. Anything `tk`/`takumi`/Odoo-customer/worktree-DB → stays in takumi-dream. Phoenix security tools fold into the Elixir recipe's session tier. |
| Q2 | **Full rewrite**, not a port of the takumi code. Proven policy *behavior* carries over as spec. |
| Q3 | **Open plane set.** Build Claude + opencode + Antigravity now; **Codex is the next one** (not yet). |
| Q4 | **Enforcement-first**, each plane's **native mechanism** ("best tool per plane" = native extension mechanism, not N reimplementations). |
| Q5 | Package ships the **universal Base only**; every project commits its own `guardrail.toml` Overlay. |
| Q6 | takumi-dream **consumes** the package post-ship. Carlitos configures takumi-dream himself — hand him steps, don't do it. |
| Q7 | Everything delivered must be **tested on claude/opencode/antigravity** (Codex later). Antigravity CLI **and IDE** both run hooks — confirmed by Carlitos hands-on. |
| Q8 | **(c) Hybrid**: one policy → (1) generated native declarative config per plane (a floor), (2) a shared **Engine** for dynamic checks. |
| Q9 | Recipes: **one per language, hard cut** — Go, Python, JS/TS, Rust, Elixir, **Odoo** (Odoo treated as a language: Python+OWL/JS+XML as one recipe). |
| Q10 | **(B)** Overlay may add/tighten, fill slots, and `waive = ["P6"]` a named rule — **logged on every hit + printed in the session-start banner**. No silent `deny`→`allow`. |
| Q11a | **Separate `agent-guardrails` repo** with its own CI. |
| Q11b | **`jq`-merge at install time** into each plane's global config; propagate via a dotfiles commit → `chezmoi apply` → `run_onchange` re-fires. Not chezmoi-managed files. |
| Q11c | **All three planes wired globally.** Antigravity global hooks: **`~/.gemini/config/hooks.json`** (installer creates if absent). Per-project `.agents/` template is only for overlays. |
| Q12 | **Both** test layers: contract fixtures (recorded payloads → expected verdict, in CI) + a thin real-agent smoke suite behind a `just` target. Green on all 3 gates a version bump. |
| Q13 | **Go, one static binary** (`guardrail`). `mvdan.cc/sh` is the deciding factor for the P3 tokenizer. |
| Q14 | **(b) Degrade, don't brick.** Engine missing/broken → lose dynamic checks (tokenizer, trifecta, symlink), the declarative floor still denies `rm -rf`/secrets/egress; session-start banner shouts the engine is down. |
| Q15 | `guardrail.toml` discovered from **`git rev-parse --show-toplevel`**, `GUARDRAIL_CONFIG` override. Committed or gitignored = project's call. |
| Q16 | **Pin both ends:** installer pins an exact release; `guardrail.toml` sets `engine_min_version`, engine **warns (not blocks)** if the binary is older. No auto-update. |
| Q17 | **(a)** One binary + per-plane **subcommands** (`guardrail hook claude` / `hook antigravity pre` / `hook codex`); opencode gets a ~30-line JS plugin that `spawnSync`s `guardrail hook opencode`. |
| Q18 | Declarative config written **at install time for the global Base** (jq-merge next to hook registration); a **`guardrail sync`** command regenerates a repo's project-level plane configs from Base+Overlay for projects that want overlays in the declarative layer too. No session-start regeneration. |
| Q19 | **(a)** Audit log = a single global stream: `~/.local/state/guardrail/audit.jsonl` (Linux/mac), `%LOCALAPPDATA%\guardrail\audit.jsonl` (Windows); path overridable per-project in `guardrail.toml`. |
| Q20 | **(B)** Prebuilt binaries from `agent-guardrails` CI releases (cross-compiled on version tags). Installer fetches the pinned asset + checksum-verifies + `Unblock-File` sweep on Windows. **No on-machine `go build`, no Docker** in the install path. Windows research verdict: unsigned Go binary is a non-issue for a CI-released binary fetched via `Invoke-WebRequest` (no Mark-of-the-Web; SmartScreen doesn't intercept `CreateProcess`); Smart App Control is the only tail risk and is near-always off on dev machines. |

## Plan series (from DESIGN.md build order)

| Plan | Deliverable | Status |
|---|---|---|
| **1** | Engine core (policy model, merge, `mvdan.cc/sh` tokenizer, verdict) + **P1/P3/P4/P9** + `guardrail hook claude`. | **DONE** — 22 commits, tag `v0.1.0-dev`, pushed. `docs/superpowers/plans/2026-09-03-engine-core-claude-adapter.md`. |
| **2** | `guardrail gen-config claude` (declarative floor: `hooks` reg + coarse `permissions`) + idempotent `settings.json` merge + `guardrail doctor` + stale-`GUARDRAIL_CONFIG` hardening. | **DONE** — Plans 1–3 shipped; see git tags `v0.1.0-dev` / `v0.2.0-dev` and `docs/superpowers/plans/` for the executed plans. |
| **3** | agent-guardrails: ADR-0004 + owned-entry hook merge, `--print=false` fix, doctor cleanups, `ci.yml` + `release.yml`, first real release `v0.3.0`. | **DONE** — `docs/superpowers/plans/2026-09-04-owned-entry-merge-and-ci-release.md`. |
| **3b** | chezmoi installer + doctor unmarked-entry warning + `make smoke`. | **agent-guardrails side DONE** (`v0.3.1`, doctor warning, smoke harness). **chezmoi side: committed on branch `add-guardrail-installer`, awaiting Carlitos review/merge/`chezmoi apply`** (the repo is private, so the installer's release download 404s until it goes public). Plan: `docs/superpowers/plans/2026-09-04-chezmoi-installer-and-smoke.md`. |
| **4** | Remaining policy modules — **P2** git-safety, **P5** fs-scope + runs-later, **P6** egress + supply-chain, **P7** injection hygiene + lethal-trifecta gate, **P10** posture file (**P7/P10** split out to **4b**). | **DONE** — P2/P5/P6 enforced by `guardrail hook claude` (Claude declarative floor extended to match); fix round also taught P1 to catch `git push --force-with-lease`, closing a Plan-1 parked item. Plan: `docs/superpowers/plans/2026-09-04-git-safety-fs-scope-egress.md`. |
| **4b** | **P7** injection hygiene + lethal-trifecta session gate + **P10** autonomy posture — separate from Plan 4 because both need a new session-state store keyed by `session_id` plus `SessionStart` wiring the Claude adapter doesn't have yet. | **DONE** — v1 scope confirmed and shipped: a two-signal P7 trifecta heuristic (private-data-access + network-attempt, session-scoped, ask-only, waivable — third leg/untrusted-content classification deferred) and the `SessionStart` autonomy-posture + active-waiver banner (P10). Plan: `docs/superpowers/plans/2026-09-04-trifecta-and-posture.md`. The `git -C <path>` target-validation gap rides with whatever future plan revisits P2's git module. |
| 5 | opencode adapter — `guardrail hook opencode` + the JS plugin + `opencode.json` permission-block generation. | **DONE** — `guardrail hook opencode` reuses the full shared pipeline (audit `Plane` field, trifecta, waivers); `gen-config opencode` generates the permission floor from the existing glob lists and deploys the embedded plugin (`internal/genconfig/opencode_plugin.js`); ask/deny → throw, allow → passthrough (ADR-0007). Plan: `docs/superpowers/plans/2026-09-04-opencode-adapter.md`. Task-1 empirics (compact): file-tool field names 4-way fallback (`filePath ?? path ?? dirPath ?? directory`, plus `sessionID ?? session_id`); absolute plugin paths in `opencode.json`'s `plugin` array work — no `file://` prefix or global plugin dir needed. Parked: **P10-for-opencode** — opencode's event hooks have no context-injection mechanism, so the posture/waiver banner has no opencode delivery path. |
| 6 | Antigravity adapter — `guardrail hook antigravity pre\|post` + `~/.gemini/config/hooks.json` generation. | **DONE** — third plane on the shared pipeline (audit, trifecta, waivers); exit code carries no meaning, `post` always emits `{}`. **No declarative floor**: Antigravity's hooks.json has no permissions concept, so the hook is the whole boundary ([ADR-0008](./adr/0008-antigravity-no-declarative-floor.md)). Plan: `docs/superpowers/plans/2026-09-04-antigravity-adapter.md`. Task-1 empirics (compact): the only proven-working hooks.json shape is a **named wrapper** per guard — `{"<guard>": {"enabled": true, <events>}}` — and that is what `AntigravityConfig` emits; payload fields `conversationId`, `toolCall.name`, `args.CommandLine`, `args.AbsolutePath`, `args.TargetFile` confirmed; `args.Cwd`/`workspacePaths` unconfirmed (parse is permissive, CWD-dependent rules degrade fail-closed); hook timeouts 15s pre / 120s post; parse errors emit deny-JSON (takumi's pattern). The generated hook groups additionally carry `"id"` keys (required by owned-entry rebinding, ADR-0004) that the proven takumi file does NOT have — verify tolerance on the first live Antigravity session. |
| 7 | Recipes (Go/Python/JS-TS/Rust/Elixir/Odoo) + `guardrail sync` (folds overlays into per-repo plane config; `gen-config --overlay`). | **DONE** — P8 recipe registry (`internal/recipe/`, Go/Python/JS-TS/Rust per-edit tier; Odoo/Elixir + session-completion tier deferred per [ADR-0009](./adr/0009-recipe-scope.md)), lenient-on-absence/strict-on-failure execution wired into the shared hook pipeline (post-edit only, allow-only escalation), contract fixtures; `guardrail sync` regenerates a project's plane configs from Base+Overlay (per-plane warn-and-continue), usage text now lists all three planes. P8 denial surfaces on Claude today (opencode needs a `tool.execute.after` plugin hook; antigravity post responses are always `{}` per ADR-0008 — post denials are audit-only). Plan: `docs/superpowers/plans/2026-09-04-recipes-and-sync.md`. |
| **Remediation Phase 1** | Self-protection and surgical fixes for CR-1, CR-2, CR-7, CR-8, CR-12, CR-14, CR-15, CR-16, M-1, and M-9 from the [adversarial review](./reviews/2026-09-04-adversarial-review.md), plus the permanent adversarial corpus. | **PUBLISHED** — tag `v0.9.0-dev` is pushed. Tasks 10/10b remain on the separate chezmoi branch `guardrail-remediation-phase1`, still unmerged, unapplied, and unpushed; the installer pin remains `v0.7.0-dev`. Plan: `docs/superpowers/plans/2026-09-04-remediation-phase1.md`. |
| **Remediation Phase 2** | Token normalization part 2: command-directory tracking, redirect-only statements, git flag/refspec parsing, fail-closed egress parsing, pipeline-wide fetch-to-interpreter detection, docker parsing, and remaining wrapper/primitive coverage. | **OUTSTANDING — NEXT.** Criticals remain: CR-3 `cd` tracking, CR-4 redirect-only statements, CR-5/CR-6 git, CR-10/CR-11 egress, CR-13 docker, and H-1 deny→ask downgrade. |
| **Remediation Phase 3** | Overlay trust model: operator-scoped waiver and audit controls, size limits, and model-facing text sanitization. | **LANDED** — ADR-0010 and implementation are complete; `v0.11.0-dev` is the target, with publication pending final task and whole-phase reviews. H-5 remains open: outside-repository symlink laundering is not the Overlay `safe_roots` issue fixed here. |
| **Remediation Phase 4** | Invert path/write allowlists, default-deny unknown pre-tool names, gate native web tools, and address remaining matching false positives. | **OUTSTANDING** — implementation not started. |
| — | Handoff doc for Carlitos to point takumi-dream at the package (he runs it). | Not written |
| — | Codex adapter — when Carlitos adopts Codex. | Deferred by Carlitos |

## How to resume

Current remediation resume (2026-09-05):

1. Phase 1 is published at `v0.9.0-dev`; Phase 3 has landed and targets
   `v0.11.0-dev`, with publication pending final task and whole-phase reviews.
2. Phase 2 is next and outstanding. Its criticals are CR-3 `cd` tracking, CR-4
   redirect-only statements, CR-5/CR-6 git, CR-10/CR-11 egress, CR-13 docker,
   and H-1 deny→ask downgrade.
3. Keep the installer at `v0.7.0-dev` while those Phase 2 criticals remain. The
   chezmoi branch `guardrail-remediation-phase1` remains unmerged, unapplied,
   and unpushed.
4. Phase 4 remains outstanding. H-5 outside-repository symlink laundering also
   remains open; Phase 3 fixed Overlay `safe_roots` containment, not H-5.

### Historical pre-remediation resume notes

The remainder of this section preserves the earlier plan-series resume record;
it is not the current remediation status.

Plans 1–4, the `v0.4.1` git -C/-c hotfix, Plan 4b, the opencode adapter (Plan 5),
and the Antigravity adapter (Plan 6)
are DONE on the agent-guardrails side (tags `v0.1.0-dev` / `v0.2.0-dev` / `v0.4.0-dev` /
`v0.4.1`;
releases [`v0.3.0`](https://github.com/CtrlCarlitos/agent-guardrails/releases/tag/v0.3.0)
and [`v0.3.1`](https://github.com/CtrlCarlitos/agent-guardrails/releases/tag/v0.3.1) are out;
pushed to `origin/main` at `github.com/CtrlCarlitos/agent-guardrails`, still **private** —
the installer's anonymous release download 404s until the repo goes public, Carlitos's call).
The executed plans live in `docs/superpowers/plans/`.

1. `cd ~/projects/CtrlCarlitos/agent-guardrails`
2. `export PATH=$PATH:/usr/local/go/bin` (or use the full path everywhere).
3. Review and merge the `add-guardrail-installer` branch in `~/.local/share/chezmoi` (6 commits). Then run `chezmoi init` **before** any `chezmoi diff`/`chezmoi apply` — the new `install_guardrail` config key makes diff/apply fail with `map has no entry for key` until init re-prompts and writes it into the live config. Where the install succeeded (repo public, Linux/WSL), `guardrail doctor` shows `guardrail hook registered` with no WARNING; on machines where it did not (macOS fails closed for now, or the private-repo 404), it will not. (The installer stays pinned to `v0.4.1` until a deliberate, reviewed bump retargets it.)
4. ~~Next: Plan 7 (recipes + `guardrail sync`)~~ — **Plan 7 is DONE.** The original plan series
   (Plans 1–7, the v0.4.1 hotfix, the deployment plan) is fully implemented **and fully deployed**:
   all three planes (Claude Code, opencode, Antigravity) are live on Carlitos's machine, verified
   against real sessions. What's genuinely open going forward:
   - **Codex adapter** — deferred by Carlitos until he adopts Codex (Q3), not started.
   - **Odoo/Elixir recipes + the session-completion tier** — follow-ups per
     [ADR-0009](./adr/0009-recipe-scope.md).
   - **Parked hardening tail** carried since Plan 1: `docker … | xargs`, backslash-escaped words,
     `bash -lc`, `git -C <path>` target-repo validation, Windows-path engine semantics, macOS
     `sha256sum` fallback, opencode native grep/glob tool mapping (plus P10-for-opencode —
     no context-injection mechanism in opencode's event hooks).

Cross-project working-style preferences now live in `~/.claude/CLAUDE.md` (global) —
they load automatically, no need to copy the `carlitos-working-style` memory.

## Policy contents reference (P1–P12)

P1 destructive-command gate · P2 git-safety gate · P3 shell-operator-aware **tokenized** fail-closed matching (`mvdan.cc/sh`, never regex — the team already concluded regex "cannot be made safe") · P4 secret-path denial (read+write+`cat`/`sed`/`grep`) + symlink-escape · P5 filesystem-scope + "runs-later" file gate (CI config, self-config `.claude/**`/`CLAUDE.md`/`.envrc`, lockfiles, `Dockerfile`/`Makefile`/`*.tf`) · P6 network-egress + supply-chain (deny non-allowlisted `curl`/`wget`, `curl|sh`, registry-redirect; installs → ask) · P7 prompt-injection hygiene (`jq -n --arg`, `readlink -f`, **lethal-trifecta session gate**) · P8 post-edit format+lint, tiered per-edit/session (Recipes) · P9 audit log (JSONL 0600, global stream, redaction, rotation) · P10 autonomy posture (advisory instruction file) · **P11 (overlay)** SQL-mutation guard · **P11 (overlay)** docker container-naming scheme · **P12 (later)** per-worktree scoped-config generator.

Deletions: don't resurrect the heredoc preprocessor; no argument-constraining URL regex; drop `antigravity-guard.py`'s hand-copied `SENSITIVE_PATTERNS`.

Known gaps to track: H-5 outside-repository symlink laundering; `docker … | xargs docker rm -f` bypass; `gitSubcommand` skips `-C`/`-c` VALUE flags (fixed in v0.4.1), but validating what `-C <path>` points at (operating on a different repo) remains parked — rides with the future plan revisiting P2's git module; backslash-escaped words; `bash -lc`; Windows-path engine semantics; macOS `sha256sum` fallback; unverified subagent/`task`-tool interception on opencode (OpenCode #5894); P10-for-opencode — no context-injection mechanism in opencode's event hooks, so the posture/waiver banner can't be delivered there (see Plan 5 row above).

## Research artifacts (scratchpad — may not survive; findings are folded into DESIGN.md)

- `guardrail-research.md` — ~55 adoptable guardrail rules + P8 toolchains per language, with source URLs
- `antigravity-hooks-research.md` — Antigravity global hooks confirmed at `~/.gemini/config/hooks.json`; events `PreToolUse/PostToolUse/PreInvocation/PostInvocation/Stop`; decisions `allow|deny|ask|force_ask|deny_unless_prior_grant`
- `windows-binary-trust.md` — unsigned Go binary is a non-issue; details in Q20 above

## Open follow-ups (not blocking)

- **Antigravity skills-path confusion**: the `skills` CLI writes `~/.agents/skills/`, but Antigravity docs say it reads `~/.gemini/config/skills/`; superpowers/mp ship skills inside their plugin trees. Carlitos sees skills working in Antigravity but doesn't know via which path. Separate investigation, unrelated to hooks.

## Memories saved this session

`~/.claude/projects/-home-carlitos--local-share-chezmoi/memory/`:
- `agent-guardrails-project.md` (type: project)
- `carlitos-working-style.md` (type: feedback) — fewer/faster questions after research; hand him steps for his other repos, don't touch them
